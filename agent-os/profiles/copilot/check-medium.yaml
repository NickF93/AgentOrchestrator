id: copilot/check-medium
owner: NickF93
version: "1.0.0"
status: active
target_runtime: copilot
intended_task_class: check
default_effort: medium
default_context_policy: focused
expected_prompt_id: checkpoint-closure-review
expected_result_protocol_id: check-result-v1
adapter_notes:
  - Use .github/copilot-instructions.md as a thin runtime entrypoint, then resolve canonical governance from AGENTS.md and workflow authorities.
  - Keep runtime-specific instruction tuning in the entrypoint or profile layer, not in the shared prompt.
