id: claude/check-medium
owner: NickF93
version: "1.0.0"
status: active
target_runtime: claude
intended_task_class: check
default_effort: medium
default_context_policy: focused
expected_prompt_id: checkpoint-closure-review
expected_result_protocol_id: check-result-v1
adapter_notes:
  - Use CLAUDE adapter files to locate canonical authorities before acting.
  - Keep tool choices and execution tuning in the adapter layer, not in the prompt.
