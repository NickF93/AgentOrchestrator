id: kilo/check-medium
owner: NickF93
version: "1.0.0"
status: active
target_runtime: kilo
intended_task_class: check
default_effort: medium
default_context_policy: focused
expected_prompt_id: checkpoint-closure-review
expected_result_protocol_id: check-result-v1
adapter_notes:
  - Use AGENTS.md as the portable Kilo entrypoint, then resolve canonical governance from the workflow authorities.
  - Keep runtime-specific tooling behavior in the adapter layer, not in the prompt.
  - Kilo support is profile-driven in v1; do not introduce a dedicated KILO.md authority file.
