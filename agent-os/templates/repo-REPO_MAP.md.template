# AUTO-GENERATED from Level-0 template. Do not edit manually.

# REPO_MAP.md

## Repository Map
Repository: {{REPO_NAME}}
Owner: {{OWNER}}
Date: {{DATE}}
last_validated_on:
validated_by:
freshness_window_days: 30

## Freshness Contract

This map is subject to the Level-0 freshness policy (see lifecycle.md).

- **Staleness check**: `today - last_validated_on > freshness_window_days`.
- **Checkpoint gate**: stale REPO_MAP blocks `verified -> done` for checkpoint items.
- **Update duty**: after any refresh trigger, update `last_validated_on` to
  the current date and `validated_by` to your identifier.
- **Automation**: `validate-plan.py --check-freshness` hard-fails when
  checkpoint items are in `review` or `verified` state and the map is stale
  or missing `last_validated_on`.

## Refresh Triggers

- Any change to entry points, module boundaries, or critical interfaces
- Any test topology change (unit/integration/e2e layout)
- Release and checkpoint closure
- Major refactor impacting hot paths or fragile areas

## Entry Points
- Main app entry:
- CLI entry:
- API entry:

## Main Modules
- module_a:
- module_b:
- module_c:

## Test Map
- unit tests:
- integration tests:
- end-to-end tests:

## Hot Paths
- hot_path_1:
- hot_path_2:

## Fragile Areas
- fragile_area_1:
- fragile_area_2:
