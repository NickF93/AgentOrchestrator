# AUTO-GENERATED by sync-workspace.sh from Level-0 templates. Do not edit manually.

# Workspace AGENTS

Generated on: {{DATE}}
Control plane: {{CONTROL_PLANE_ROOT}} ({{CONTROL_PLANE_REF}})

This workspace-level file is a runtime materialization layer.
Canonical governance lives in the Level-0 control plane repository.

## Authority Precedence

Priority order for conflict resolution:

1. Explicit human instructions
2. `AGENTS.md` — workflow authority and operating rules
3. `ARCHITECTURE.md` — software constraints and boundaries
4. `PLAN.yaml` — active execution tracking
5. Code, tests, and real artifacts — implementation evidence
6. Generated views (`PLAN.md`, `PLAN.dot`) — informative, not authoritative

## Canonical Concern Split

| Concern | Canonical Authority |
|---|---|
| Workflow and operating rules | `AGENTS.md` |
| Software constraints and boundaries | `ARCHITECTURE.md` |
| Active execution tracking | `PLAN.yaml` |
| Human-readable plan view | `PLAN.md` (generated) |
| Graph plan view | `PLAN.dot` (generated) |
| Codebase topology | `REPO_MAP.md` |

## Single-Writer Rule

One orchestrator agent owns and writes the canonical PLAN source.
Other agents may propose changes or produce evidence but must not write
directly to the canonical PLAN.

## Commit Message Contract

All commits MUST use: `<type>(<scope>): <description>`

Optional body and footer sections are strongly suggested, especially for
medium or large commits.

## Escalation

Escalate to the human owner when:

- A change would alter authority boundaries
- A conflict exists between `ARCHITECTURE.md` and `PLAN.yaml`
- A schema change would invalidate existing plan data
- Irreversible Git automation is requested beyond the current phase

## Runtime References

- Workflow model: `{{CONTROL_PLANE_ROOT}}/agent-os/workflow/`
- Plan schema: `{{CONTROL_PLANE_ROOT}}/agent-os/schemas/plan.schema.json`
- Templates: `{{CONTROL_PLANE_ROOT}}/agent-os/templates/`
- Scripts: `{{CONTROL_PLANE_ROOT}}/agent-os/scripts/`
