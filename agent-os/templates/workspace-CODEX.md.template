# AUTO-GENERATED by sync-workspace.sh from Level-0 templates. Do not edit manually.

# Workspace Codex

Generated on: {{DATE}}
Control plane: {{CONTROL_PLANE_ROOT}}
CONTROL_PLANE_ROOT: {{CONTROL_PLANE_ROOT}}
Control plane ref: {{CONTROL_PLANE_REF}}
Control plane commit: {{CONTROL_PLANE_COMMIT}}

This is a thin workspace runtime entrypoint for Codex generated from the
current local control-plane checkout. It does not define governance rules.

Canonical authority is bounded in:

- `AGENTS.md`
- `ARCHITECTURE.md`
- `PLAN.yaml`
- `{{CONTROL_PLANE_ROOT}}/agent-os/workflow/*`

## Runtime Notes

- Follow `AGENTS.md` for workflow and operating rules.
- Shared Layer-0 assets resolve from `CONTROL_PLANE_ROOT`.
- Repo-tracked Python commands use `AGENT_PYTHON` from `.env`.
- If `.env` is unavailable inside the sandbox, Codex may use the active
  sandbox interpreter temporarily and report that adapter choice.
- Keep host-specific paths and procedures only in `.env`.
